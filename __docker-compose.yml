version: "3.3"
services:

  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: some_interesting_password
      POSTGRES_DB: telegram
    ports:
      - "5432:5432"
  app :
    build: .
    restart: always
    depends_on:
      - postgres
